// Palette dùng chung ở src/shared/notebook-palette (main validate + renderer picker). Re-export cho tiện dùng ở main.
export {
  PALETTE,
  DEFAULT_COLOR,
  isPaletteColor,
} from "@shared/notebook-palette";
